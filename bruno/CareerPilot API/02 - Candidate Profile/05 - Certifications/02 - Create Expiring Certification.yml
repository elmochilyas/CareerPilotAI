info:
  name: 02 - Create Expiring Certification
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/profile/items"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{frontendUrl}}"
    - name: Referer
      value: "{{frontendUrl}}/"
  body:
    type: json
    data: |-
      {
        "type": "certification",
        "title": "[Bruno Test] AWS Certification",
        "organization": "Amazon Web Services",
        "start_date": "2026-01-15",
        "description": "AWS Certified Developer - Associate.",
        "metadata": {
          "issuer": "AWS",
          "credential_id": "AWS-DEV-2026-001",
          "expiry_date": "2028-01-15"
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const xsrfToken = bru.cookies.get("XSRF-TOKEN");

        if (!xsrfToken) {
          throw new Error(
            "XSRF-TOKEN cookie is missing. Run 02 - CSRF Cookie first."
          );
        }

        req.setHeader(
          "X-XSRF-TOKEN",
          decodeURIComponent(xsrfToken)
        );
    - type: after-response
      code: |-
        test("Status code is 201", function () {
          expect(res.getStatus()).to.equal(201);
        });

        test("Response is JSON", function () {
          expect(res.getHeader("content-type")).to.include("application/json");
        });

        test("Certification item was created", function () {
          const body = res.getBody();
          const item = body.data ?? body;

          expect(item.type).to.equal("certification");
          expect(item.title).to.equal("[Bruno Test] AWS Certification");
        });

        test("Expiry date is present in metadata", function () {
          const body = res.getBody();
          const item = body.data ?? body;

          expect(item.metadata.expiry_date).to.equal("2028-01-15");
        });

        const body = res.getBody();
        const item = body.data ?? body;
        bru.setVar("expiringCertificationItemId", String(item.id));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5