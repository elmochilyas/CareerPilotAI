info:
  name: 02 - Create Temporary Project Item B
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/profile/items"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
    - name: Origin
      value: "{{frontendUrl}}"
    - name: Referer
      value: "{{frontendUrl}}/"
  body:
    type: json
    data: |-
      {
        "type": "project",
        "title": "[Bruno Test] Ordering Item B",
        "start_date": "2026-06-15",
        "is_current": true,
        "description": "Another temporary project for reorder testing."
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const xsrfToken = bru.cookies.get("XSRF-TOKEN");

        if (!xsrfToken) {
          throw new Error(
            "XSRF-TOKEN cookie is missing. Run 02 - CSRF Cookie first."
          );
        }

        req.setHeader(
          "X-XSRF-TOKEN",
          decodeURIComponent(xsrfToken)
        );
    - type: after-response
      code: |-
        test("Status code is 201", function () {
          expect(res.getStatus()).to.equal(201);
        });

        test("Response is JSON", function () {
          expect(res.getHeader("content-type")).to.include("application/json");
        });

        test("Project B was created", function () {
          const body = res.getBody();
          const item = body.data ?? body;

          expect(item.type).to.equal("project");
          expect(item.title).to.equal("[Bruno Test] Ordering Item B");
        });

        const body = res.getBody();
        const item = body.data ?? body;
        bru.setVar("tempProjectBId", String(item.id));

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5